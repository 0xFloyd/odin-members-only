<!DOCTYPE html>
<html lang="en">

<%- include('header') -%>

<body>
     <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                <% if (currentUser) {%>
                <li class="nav-item">
                    <a class="nav-link" href="/log-out">Log Out</a>
                </li>
                    <% if (!currentUser.member) {%>
                        <li class="nav-item">
                            <a class="nav-link" href="/members">Member Login</a>
                        </li>
                    <% } else {%>
    
                    <%}%>
                <% } else {%>
                    <li class="nav-item">
                        <a class="nav-link" href="/sign-up">Sign Up</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/log-in">Log In</a>
                    </li>
                <%}%>
            </ul>
        </div>
    </nav>
    <div class="container">
        <div class="row justify-content-center">
            <h1>Club Memba</h1>
        </div>
        <div class="row justify-content-center">
            <div class="col-12">
                <% if (!currentUser) {%>
                    <h3>You must be logged in as a member to view this page.</h3>
                    Return <a href="/">Home</a>
                <% } else { %>
                    <% if (!currentUser.member) {%>
                        <h3>You must be logged in as a member to view this page.</h3>
                        Return <a href="/">Home</a>
                    <% } else { %> 
                        <h3><%= title + currentUser.username%>! Please enter your message.</h3>
                    <form action="" method="POST" id="messageForm">
                        <div class="form-group">
                            <label for="messageBody">Enter Message</label>
                            <br>
                            <textarea rows="4" cols="20" name="messageBody" form="messageForm"></textarea>
                        </div>
                        <button class="btn btn-primary">Post Message</button>
                    </form>
                </div>
            </div>
        <%} %>
    <%} %>
    <%- include('bootstrap-script') -%>
</body>

</html>